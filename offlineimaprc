[general]
accounts = Groups, Personal

metadata = ~/Mail/offlineimap

[Account Groups]
localrepository = GroupsLocal
remoterepository = GroupsRemote

[Repository GroupsLocal]
type = Maildir
localfolders = ~/Mail/groups

[Repository GroupsRemote]
type = Gmail
remoteuser = bvk.groups@gmail.com
folderfilter = lambda foldername: foldername in ['INBOX']

[Account Personal]
localrepository = PersonalLocal
remoterepository = PersonalRemote

[Repository PersonalLocal]
type = Maildir
localfolders = ~/Mail/personal

[Repository PersonalRemote]
type = Gmail
remoteuser = bvkchaitanya@gmail.com
folderfilter = lambda foldername: foldername in ['INBOX']

# [mbnames]
# enabled = yes
# filename = ~/Mail/muttrc.mailboxes
# header = "mailboxes"
# peritem = " +%(accountname)s/%(foldername)s"
# sep = " "
# footer = "\n"

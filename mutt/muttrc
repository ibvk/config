set realname='BVK Chaitanya'

set folder="~/Mail"
set spoolfile="+groups/INBOX"
mailboxes "+groups/INBOX" "+personal/INBOX"

folder-hook groups   "source ~/.mutt/groups"
folder-hook personal "source ~/.mutt/personal"

set mbox="+received"
set record="+sent"
set postponed="+postponed"

set edit_headers
set use_from=yes
set envelope_from=yes

set move=no
